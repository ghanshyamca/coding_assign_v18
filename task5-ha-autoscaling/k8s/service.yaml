apiVersion: v1
kind: Service
metadata:
  name: ha-app
  namespace: ha-app
  labels:
    app.kubernetes.io/name: ha-app
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: ha-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
