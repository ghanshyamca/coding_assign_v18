apiVersion: v1
kind: Service
metadata:
  name: fullstack-backend
  namespace: fullstack
  labels:
    app: fullstack-backend
spec:
  type: ClusterIP
  selector:
    app: fullstack-backend
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
